# SPEC.md

> IRIS MVP 기능 요구사항 및 동작 명세를 정리한 문서입니다.

---

# 1. MVP 목표

IRIS는 경북대학교 계열 공지 사이트를 반복해서 확인해야 하는 불편함을 줄이기 위해, 공지 사이트를 주기적으로 확인하고 필요한 공지를 Discord에서 편리하게 받을 수 있는 서비스를 목표로 합니다.

---

# 2. MVP 범위

## Landing

- 서비스 소개
- Discord Bot 초대 (초대링크 존재)
- 시작 가이드

## Admin

- 지원 사이트 프리셋 선택 및 직접 설정
- 테스트 크롤링
- 카테고리 설정

## Discord

- /help
- /guide
- /setup
- /subscribe
- /keyword

## System

- 공지 크롤링
- Discord 알림
- 키워드 DM 알림

---

# 3. Functional Requirements

## 3.1 Landing

### 서비스 소개

- 서비스 소개
- 핵심 기능 소개
- Discord Bot 초대 버튼 제공
- 시작 가이드 버튼 제공

### Discord Bot 초대

**동작**

- Discord OAuth 초대 페이지로 이동

### 시작 가이드

**동작**

- 시작 가이드 버튼은 별도 페이지나 모달을 열지 않고 랜딩의 카드 영역을 시작 가이드 단계로 전환한다.
- 시작 가이드 표시 중 버튼 문구는 `기능 소개`로 변경한다.
- `기능 소개` 버튼을 누르면 카드 영역을 핵심 기능 소개로 되돌린다.

**표시**

- 사전 준비: Discord에 가입하고 공지 알림을 받을 서버와 채널 준비
- 봇 초대하기: 랜딩의 Discord Bot 초대 버튼으로 IRIS Bot을 서버에 추가하고 필요한 권한 승인
- /setup 입력: 봇을 초대한 서버에서 관리자 권한으로 `/setup` 명령어 입력
- 관리자 페이지 접속: Bot이 제공한 링크로 관리자 설정 페이지 접속

---

## 3.2 관리자 페이지

### 공지 사이트 등록

**입력**

- 설정 방식
  - 지원 사이트: 경북대학교 계열 공지 사이트 프리셋 중 선택
  - 직접 설정: 사이트 이름, 공지 URL, Selector 직접 입력
- 프리셋 선택 시 사이트 이름, 공지 URL, Selector는 프리셋 값으로 자동 적용
- 직접 설정 시 입력 항목
  - 사이트 이름
  - 공지 URL
  - List Selector
  - Title Selector
  - Link Selector
  - Date Selector
  - Category Selector
  - Category List Selector

**기능**

- 지원 사이트와 직접 설정 모드 전환
- 프리셋 목록 표시
- 선택된 프리셋 강조 표시
- 직접 설정 모드에서 Selector 가이드 버튼 표시
- 직접 설정 모드에서 개발자에게 요청 버튼 표시
- 테스트 크롤링
- 최근 공지 미리보기
- 다음 버튼

**검증**

- 테스트 크롤링은 선택한 프리셋 또는 직접 입력한 Selector로 최신 공지를 1개 이상 가져오면 성공
- 각 공지는 title, link, date, category를 모두 포함해야 함
- 공지별 category가 없거나 전체 카테고리 목록에서 감지된 카테고리가 0개면 실패
- 테스트 크롤링 성공 전에는 다음 단계로 이동할 수 없음

---

### 카테고리 설정

**표시**

감지된 카테고리 목록

**설정 항목**

- 알림 채널
- 역할 이름
- 활성화 여부

**동작**

- 알림 채널은 기존 Discord 채널 목록에서 선택
- 카테고리 설정 화면은 Discord 채널 목록 새로고침 기능을 제공
- 관리자가 페이지를 연 뒤 Discord에서 새 채널을 만들면 새로고침으로 최신 채널 목록을 다시 조회할 수 있음
- MVP에서는 모든 카테고리 알림을 하나의 알림 채널로 전송
- API 호환을 위해 각 카테고리의 `channelId`에 같은 알림 채널 id를 저장
- IRIS는 MVP에서 Discord 채널을 자동 생성하지 않음
- 활성화된 카테고리의 역할은 사용자가 입력한 이름으로 IRIS가 자동 생성
- 같은 이름의 역할이 이미 있으면 기존 역할을 재사용하지 않고 저장을 실패 처리
- 새로 생성하는 역할은 Discord 기본 색상을 사용
- 비활성화된 카테고리는 역할을 생성하지 않고 roleId를 null로 저장
- 활성화된 카테고리만 /subscribe, 공지 저장, 공지 알림 대상이 됨

**기능**

- 이전
- 저장
- 다음

**저장 정책**

- 기존 설정이 없는 서버에서 최초 저장하면 전체 공지 사이트 설정과 카테고리 설정을 함께 생성한다.
- 기존 설정이 있는 서버에서 알림 채널, 역할 이름, 활성화 여부만 수정하면 카테고리 설정만 부분 수정한다.
- 기존 설정이 있는 서버에서 프리셋을 변경하거나 직접 설정의 사이트 정보 또는 Selector를 수정하고 테스트 크롤링을 다시 실행한 뒤 저장하면 전체 설정을 교체한다.
- 저장 또는 삭제 중 기존 설정이 이미 삭제된 상태를 감지하면 사이트 등록 단계로 돌아간다.

**삭제**

- 기존 설정이 있는 경우 관리자는 관리자 페이지에서 설정 삭제를 실행할 수 있다.
- 설정 삭제는 공지 사이트 설정, 카테고리 설정, 저장된 공지 이력, 구독 정보를 초기화한다.
- 설정 삭제 시 키워드 설정은 유지한다.
- 설정 삭제 후 관리자는 사이트 등록 단계부터 다시 설정한다.

---

### 설정 완료

**표시**

- 설정 완료 안내
- 사용 가능한 명령어
- 공지 사이트와 공통 알림 채널
- 카테고리별 역할 이름과 활성화 여부 요약
- 카테고리 연결 목록이 5개를 초과하면 접기/펼치기 제공

**기능**

- 첫 페이지로 이동

---

## 3.3 Discord

### /help

**기능**

- 사용 가능한 명령어 안내
- 보라색 Embed로 표시
- Ephemeral 응답
- 관리자 전용 명령어 표시

---

### /guide

**기능**

- 권한 설정, 알림 방식, 개인정보 설정, 설정 삭제, Bot 제거 주의사항 안내
- Discord 개인정보 설정에서 서버 멤버의 DM을 차단하면 키워드 알림을 받을 수 없고, 공지를 DM으로 저장할 수 없음을 안내
- Discord 역할까지 정리하려면 Bot 제거 전에 관리자 페이지에서 설정 삭제를 먼저 실행해야 함을 안내
- Bot을 먼저 제거하면 DB 데이터는 정리되지만 Discord 역할은 서버에 남을 수 있음을 안내
- 보라색 Embed로 표시
- Ephemeral 응답

---

### /setup

**기능**

- 관리자 권한 확인
- 관리자이면 관리자 페이지 바로가기 버튼 제공
- 정상 응답은 보라색 Embed와 링크 버튼으로 표시
- MVP에서는 관리자 페이지 링크에 별도 인증 토큰을 포함하지 않음
- 응답은 Ephemeral로 제공

---

### /subscribe

**기능**

- 활성화된 카테고리 조회
- 정상 응답은 보라색 Embed와 카테고리 버튼으로 표시
- Embed 내용은 짧은 안내만 표시하고 버튼 영역을 중심으로 구성
- Discord 버튼 컴포넌트로 카테고리 구독 상태 표시
- 카테고리 버튼 클릭 시 해당 카테고리 구독/해제 토글
- Discord 역할 자동 부여
- Discord 역할 자동 제거

---

### /keyword

**기능**

- 키워드 목록 조회
- 정상 응답은 보라색 Embed로 표시
- 키워드 추가 모달 제공
- 키워드 삭제 선택 메뉴 제공
- 삭제할 키워드를 여러 개 선택한 뒤 `삭제하기` 버튼으로 삭제 확정
- `나가기` 버튼으로 삭제하지 않고 키워드 목록으로 복귀
- 키워드는 `guildId + userId + keyword` 기준으로 서버별 저장
- 공지 사이트 설정이 없어도 키워드는 등록할 수 있으며, 설정 완료 후 새 공지부터 알림 대상이 된다

---

## 3.4 System

### 공지 크롤링

**기능**

- 등록된 사이트 주기적 크롤링
- MVP 기본 주기 30분
- 공지 사이트 설정 저장 또는 교체 직후 해당 사이트 초기 수집
- 초기 수집은 기준 데이터 저장만 수행하고 Discord 알림은 보내지 않음
- 신규 공지 확인
- noticeSiteId와 normalizedLink 기반 hashKey 중복 검사

---

### Discord 알림

**기능**

- 공통 알림 채널 전송
- Discord 역할 Mention
- 실제 공지 카테고리 역할과 활성화된 `전체` 역할을 함께 mention할 수 있음
- Embed 메시지
- 제목, 날짜, 원본 링크 포함
- `DM으로 저장` 버튼 제공
- `DM으로 저장` 클릭 시 해당 공지 Embed를 클릭한 사용자 DM으로 전송
- DM으로 복사된 공지 알림에는 `요약 보기` 버튼과 2단계 삭제 버튼 제공
- `요약 보기` 버튼 제공
- MVP 현재 `요약 보기`는 실제 요약을 생성하지 않고 준비 중 안내만 Ephemeral로 응답

---

### 키워드 알림

**기능**

- 제목 기준 키워드 검사
- 일치 시 사용자 DM 전송
- 원본 링크 포함
- DM 알림에는 `알림 삭제` 버튼을 제공
- DM 알림에는 `요약 보기` 버튼도 함께 제공
- `알림 삭제` 클릭 시 바로 삭제하지 않고 `정말 삭제` / `취소` 버튼으로 2단계 확인
- `정말 삭제` 클릭 시 해당 DM 메시지만 삭제
- DM 메시지 삭제는 공지 데이터나 키워드 설정을 변경하지 않음

---

### Bot 제거 정책

- 관리자가 설정 삭제 후 Bot을 제거하는 것을 권장합니다.
- Bot이 먼저 제거되면 `guildDelete` 이벤트를 통해 해당 서버의 공지 설정, 구독, 키워드 데이터를 자동으로 삭제합니다.
- Bot이 이미 제거된 상태에서는 Discord 역할을 삭제할 수 없습니다.
- 사용자가 서버를 나가면 `guildMemberRemove` 이벤트를 통해 해당 서버에서 그 사용자의 구독과 키워드를 삭제합니다.

---

### AI 공지 요약

**MVP 정책**

- 1차 MVP 필수 기능은 아님
- 확장 가능한 구조만 준비
- 기본 알림에서는 요약을 자동 생성하지 않음
- MVP 현재는 사용자가 요약 버튼을 눌러도 AI 요약을 생성하지 않고 준비 중 안내만 제공
- 불필요한 토큰 소비를 줄이기 위해 요청 기반으로 동작
- 향후 실제 요약 기능은 요청 기반으로 동작
- 향후 요약 생성 실패 시 원본 공지 정보만 유지

**기능**

- 공지 알림 Embed에 요약 보기 버튼 제공 가능
- MVP 현재는 버튼만 제공하고 `요약 기능은 준비 중입니다.` 안내를 Ephemeral로 반환
- 향후 버튼 클릭 시 공지 상세 본문을 기반으로 AI 요약 생성
- 향후 생성된 요약은 사용자에게 Ephemeral 응답 또는 후속 메시지로 제공
- 향후 생성된 요약은 재사용할 수 있도록 저장 가능

---

# 4. Out of Scope

현재 MVP에서는 구현하지 않습니다.

- AI Selector 추천
- 여러 공지 사이트 등록
- 관리자 Dashboard
- Google Calendar 연동
- 공지 마감일 추출 및 재알림
- 알림 시점의 자동 AI 요약 생성

---

# 5. Future Enhancements

- AI Selector 추천
- Multi-site 지원
- Dashboard
- Google Calendar 연동
- AI 기반 마감일 추출 및 재알림
